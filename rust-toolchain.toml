[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
# greenlit-runtime's build.rs sub-builds greenlit-init for static musl;
# provisioning the target here keeps host, CI, and job containers aligned.
targets = ["x86_64-unknown-linux-musl"]
